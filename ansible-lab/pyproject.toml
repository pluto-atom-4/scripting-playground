[project]
name = "ansible-lab"
version = "0.1.0"
description = "Ansible practice lab for UW-IT managed services (Ovid & Vergil)"
requires-python = ">=3.11,<3.12"
dependencies = [
    "ansible>=9.0,<11",
]

[project.optional-dependencies]
mysql = [
    "pymysql>=1.1",
]
